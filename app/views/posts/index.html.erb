<% provide(:title, 'Posts') %>
<h1>Posts</h1>
<p><%= link_to 'New post?', new_post_path %></p>
<% @posts.each do |post| %>
	<li><%= link_to post.title, post_path(post) %> | <%= link_to 'Delete', post_path(post), method: :delete, data: { confirm: 'Are you sure?' } %></li>
<% end %>