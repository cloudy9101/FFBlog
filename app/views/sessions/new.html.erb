<% provide(:title, 'Sign in') %>
<h1>Sign in</h1>

<div>
	<%= form_for :sessions, url: sessions_path do |f| %>
		<%= f.label :email %>
		<%= f.text_field :email %>
		<br>
		<%= f.label :password %>
		<%= f.password_field :password %>
		<br>
		<%= f.submit 'Log in' %>
	<% end %>
</div>